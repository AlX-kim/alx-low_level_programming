#include <stdio.h>


/**
* main - Evaluating random numbers
*
* Return: Always 0 (Success)
*/

int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
if (!= 'e' && c != 'q')
putchar (a);
putchar ('\n');
return (0);
}

