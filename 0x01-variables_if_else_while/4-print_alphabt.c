#include <stdio.h>


/**
* main - Evaluating random numbers
*
* Return: Always 0 (Success)
*/

int main(void)
{
char a = 'a';

if (a != 'q' && 'e')
{
while (a <= 'z')
{
putchar (a);
}
putchar ('\n');
return (0);
}
